--extra-index-url https://download.pytorch.org/whl/cu126

fastapi
uvicorn
httpx
faster-whisper
python-multipart
pyannote.audio
python-dotenv
av

# GPU: must match; plain `pip install torch` gets CPU-only
torch==2.8.0+cu126
torchaudio==2.8.0+cu126
torchvision==0.23.0+cu126
